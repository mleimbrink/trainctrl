[package]
name = "trainctrl_tracks"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
readme = "../readme.md"
publish = false

[dependencies]
pathfinding = { version = "4.15" }
serde = { version = "1.0", default-features = false, features = ["derive"] }
serde_json = { version = "1.0", default-features = false, features = ["std"] }
